#include<stdio.h>
int main()
{
     int a,b,i,sum=0;
     while(scanf("%d %d",&a,&b)=1)
     {
         for(i=a;i<=b;i++)
         {
             sum=sum+i;
         }
         printf("Sum of %a to %d is -> %s;",a,b,sum);
     }
    return 0;
}

